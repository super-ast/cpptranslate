#include <vector>
#include <string>
using namespace std;
typedef vector<int> VI;
typedef vector<VI> MI;

void f(int x) {}
int main() {
  VI v;
  v.size();
  v.resize(0, 0);
  f(0);

  string s;
  s.size();
}
